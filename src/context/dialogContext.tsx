// import { createContext } from 'react';

// import { ContentStatus, EpicDialogProps } from 'components/Dialog/DialogContainer';

// type ProviderContext = readonly [(option: EpicDialogOption) => void, () => void];

// type EpicDialogOption = ContentStatus & Omit<EpicDialogProps, 'open'>;

// const dialogContext = createContext<ProviderContext>(null as unknown as ProviderContext);

// export default dialogContext;
export {}
